# servarr

Content of .env
```
GANDIV5_API_KEY=  # GANDI API KEY 
ACME_EMAIL=       # Email used to receive expiration notifications for your SSL certs
TZ=               # Timezone 
SUBDOMAIN=        # Subdomain to append to your hosts
PUID=             # User ID used to access files on the host
PGID=             # Group ID used to access files on the host
MEDIA_DIR=        # Media directory path
DOWNLOAD_DIR=     # Download directory path
```
